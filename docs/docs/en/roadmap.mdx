---
pageType: doc
title: Roadmap
---

import {
  Page,
  PageHero,
  KpiStrip,
  RoadmapTimeline,
  Section,
  Callout,
  NextCta,
  BottomNote,
} from '../../components/PageChrome';

<Page>
  <PageHero
    eyebrow="// ROADMAP · v1"
    title="Phases, not deadlines."
    sub="From Phase 0 to Phase 7, ordered by what shipped — each phase is a unit of reproduction primitive added on top of the previous, not a calendar target."
  />

  <KpiStrip
    items={[
      { value: '8', label: 'Phases queued', accent: 'teal' },
      { value: '7', label: 'Phases closed', accent: 'teal' },
      { value: '11', label: 'Recipes shipped', accent: 'violet' },
      { value: '5', label: 'MCP tools', accent: 'coral' },
    ]}
  />

  <Section
    eyebrow="// 00 · A NOTE ON DATES"
    heading="No due-date column."
  >
    <p>
      Phase durations on this project are directional — months to years — not commitments. Inventing calendar targets for a lifelong project would trade honesty for the appearance of progress, and would drive every later decision toward hitting a number rather than reproducing bugs correctly.
    </p>
    <Callout>
      When a phase acquires a real deadline, the date will land alongside the phase, explained. Until then, the ordering and the "what shipped" signals are the roadmap.
    </Callout>
    <p>
      Phase titles mirror the GitHub Milestones; the Milestones are the canonical planning surface, and this page is the user-facing translation.
    </p>
  </Section>

  <Section
    eyebrow="// PHASES"
    heading="What has shipped, what is shipping, what is queued."
  >
    <RoadmapTimeline
      statusLabels={{ closed: 'CLOSED', active: 'ACTIVE', planned: 'PLANNED' }}
      shippedHeading="What shipped"
      remainingHeading="What's left"
      phases={[
        {
          number: '00',
          title: 'Bootstrap',
          status: 'closed',
          statusNote: '2026-04-26',
          lead: "Infrastructure-as-Code foundations, the AI-delegated development workflow, and the initial vision / architecture / roadmap stubs. Scaffolding only — no product code yet.",
          shipped: [
            'GitHub settings, labels, milestones, branch protection — all under OpenTofu',
            'AI-commit + human-review workflow codified in CLAUDE.md / AGENTS.md',
            'Three-layer src/ tree (layer1_wasm / layer2_docker / layer3_thirdway)',
          ],
        },
        {
          number: '01',
          title: 'Layer 1 — data processing',
          status: 'closed',
          lead: 'The first real reproduction vertical: Python + SQLite over WebAssembly via Pyodide. Bug description → reproduction page → click → pass/fail verdict.',
          shipped: [
            'pandas / numpy / cpython behavioural regressions reproducible from a single browser tab',
            'Established the Page Contract v1 (verdict.json wire shape)',
            'Common loader + verdict-envelope helpers under _shared/ so later recipes scale instantly',
          ],
        },
        {
          number: '02',
          title: 'Layer 1 — multi-language',
          status: 'closed',
          lead: 'Layer 1 broadens: Rust (wasm32-wasip1), Ruby.wasm, php-wasm. Adding a language reduces to "write one runtime adapter" via the shared scaffolding.',
          shipped: [
            'regex#779 (Rust), ruby#21709, php#12167 shipped',
            'php_loader.ts / ruby_loader.ts / rust_loader.ts factored into the shared module — adding a new runtime is now a small adapter',
          ],
        },
        {
          number: '03',
          title: 'Layer 2 — Docker',
          status: 'closed',
          lead: 'Full-fidelity container layer for bugs that need real fork / real sockets / real filesystems. Vivarium does not host execution; the catalogue ships and visitors `docker run`.',
          shipped: [
            'Four Layer 2 recipes (postgres-lost-update / flock-is-advisory / find-xargs-whitespace / bash-local-shadows-exit)',
            'Pre-built images on GHCR; CI-snapshot verdicts embedded in each gallery page',
            '"Open in Codespaces" one-click bring-up established',
          ],
        },
        {
          number: '04',
          title: 'Layer 3 — record-replay',
          status: 'closed',
          statusNote: '2026-04-28',
          lead: "Third layer for true heisenbugs L1 / L2 can't reach. Trace baked into a GHCR image; visitors `rr replay` to rewind deterministically.",
          shipped: [
            'pthread lost-update shipped (parallel mergesort interleave dependency)',
            'Recording is offline-only on PMU-equipped hosts; replay runs on commodity Linux for visitors and CI alike',
          ],
        },
        {
          number: '05',
          title: 'Ecosystem',
          status: 'closed',
          statusNote: '2026-04-29',
          lead: 'Contract v1 / Manifest v1 published as external standards with JSON Schema and CI enforcement. The phase that gave Vivarium a public spec.',
          shipped: [
            'Contract v1 (verdict.json wire shape) published as JSON Schema',
            'Manifest v1 + three reference TOMLs — declaring a reproduction takes one file',
            'Reusable verdict-capture workflow + weekly cron drift detection',
          ],
        },
        {
          number: '06',
          title: 'Usability and visual layer',
          status: 'closed',
          statusNote: '2026-05-02',
          lead: 'Built the human-facing interaction layer above the primitives. All six sub-streams (V, R, S, M, X, L) shipped at v1; the closure rule was satisfied four times over.',
          shipped: [
            'Stitch-driven visual redesign + reusable component library',
            'Full EN ⇄ JA i18n (symmetric trees + locale switcher)',
            'Contract v1 evidence surface + branch-fix verdict capture pipeline + side-by-side comparison page',
            'Faceted gallery + paste-an-error matcher + interactive manifest scaffolder',
            'Vivarium MCP server (list_recipes / get_recipe / lookup_verdict / match_error)',
          ],
        },
        {
          number: '07',
          title: 'First-30-minutes onboarding',
          status: 'active',
          lead: 'Turn "primitives are usable" into "a stranger can pick this up cold". V′ (UI brush-up) and D (documentation) are co-defined — where the docs hit a wall, the UI is wrong. Closure is permissive: V′ + D + B3.',
          shipped: [
            'D — getting-started / write-your-first-reproduction / integrate-with-your-repo / use-from-ai-agent / compare-branch-fix / glossary + bonus fork-your-own (EN+JA, 7 pages)',
            'B3 — R.2 Path A on php-12167 + 5th MCP tool `verify_branch_fix` + the AI-slop verification walkthrough',
            'A5 — match_error v2 (synonyms / fuzzy / multi-language stopwords)',
            'A6 — recipes-index / branch-fix-pipeline / recipes.schema editorial follow-up',
            'V′ — landing / /repro/ / /repro/compare / architecture page redesigns (in progress)',
          ],
          remaining: [
            "V′ remaining: this page (roadmap) / overview / spec / match / new guide/index + nav reorder",
            'Component graduation — extract `VerdictBadge` / `LayerPill` as standalone exports',
            'A3 — ajv-standalone migration (waiting for trigger; optional)',
          ],
        },
      ]}
    />
  </Section>

  <Section
    eyebrow="// MEASUREMENT"
    heading="Closure rule: partial completion is acceptable."
  >
    <p>
      Phase closure on this project follows a deliberate pattern set by the Phase 4 close ADR (ADR-0012). When the shape of the phase is <strong>proven by partial completion</strong> — one Layer 3 recipe, one MCP tool, one external adopter — the phase closes and remaining work is requeued. This protects the lifelong project from the slow-creep of incomplete phases that never close.
    </p>
    <p>
      The decisions that did <em>not</em> ship are recorded with the same care as the ones that did. Public phase-close briefs cite the private ADRs that captured the trade-offs, so future contributors understand <em>why</em> a path was rejected, not just <em>that</em> it was.
    </p>
  </Section>
</Page>

<NextCta
  eyebrow="// NEXT"
  heading={
    <>
      Try it yourself{' '}
      <span className="v-next-cta__heading-arrow">→</span>
    </>
  }
  sub="Once the phase shape is clear, the fastest way forward is to open one recipe in a browser tab and read the verdict."
  primary={{ label: 'First five minutes →', href: '/vivarium/guide/getting-started' }}
  ghost={{ label: 'Architecture', href: '/vivarium/architecture' }}
/>

<BottomNote
  text="VIVARIUM IS PART OF ALETHEIA-WORKS · OPEN THE SOURCE ON GITHUB →"
  href="https://github.com/aletheia-works/vivarium"
/>
